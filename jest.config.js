module.exports = {
  testEnvironment: "node", // Indique l'environnement de test (Node.js dans ce cas)
  verbose: true, // Affiche les détails des tests
  collectCoverage: true, // Active la collecte de la couverture de code
  coverageReporters: ["text", "html"], // Spécifie les formats de rapport de couverture
};
